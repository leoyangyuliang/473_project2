import React, { Component } from "react";
import ReactDOM from 'react-dom';

class ChatList extends Component {
  render() {
    return (
      <div>
        ChatList will goes here
      </div>
    );
  }
 }
export default ChatList;
